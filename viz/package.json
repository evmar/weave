{
  "name": "viz",
  "scripts": {
    "serve": "esbuild --bundle --target=es2020 --sourcemap --outdir=. '--external:*.ttf' bundle=viz.tsx style=viz.css --serve=:7761 --servedir=.",
    "bundle": "esbuild --bundle --target=es2020 --sourcemap --outdir=. '--external:*.ttf' bundle=viz.tsx style=viz.css",
    "test": "node --import tsx --test *_test.ts"
  },
  "dependencies": {
    "d3": "^7.4.3",
    "preact": "^10.29.8",
    "webtreemap": "^2.0.1"
  },
  "devDependencies": {
    "@types/d3": "^7.1.0",
    "esbuild": "^0.28.2"
  }
}
